---
import { Image } from "astro:assets";
import aboutImage from "@/assets/img/marcello-and-tato.jpg";
---

<section class="about">
  <div class="about-container">
    <p>
      I'm an independent full-stack engineer and architect, I lead teams of
      developers and designers implementing creative designs with a strong focus
      on <em>accessibility</em>, <em>sustainability</em>, and <em
        >performance</em
      >.
    </p>

    <Image src={aboutImage} alt="Marcello and Tato" />
    <!--    <img src="/img/marcello-and-tato.jpg" alt="Marcello and Tato" class="about-picture"/>-->
  </div>
</section>

<style lang="scss">
  @use "../styles/shared" as *;

  .about {
    min-height: 100vh;
    margin: 40vh 0;
  }

  .about-container {
    /*@include container;*/
    display: grid;
    grid-template-columns: 100%;
    grid-template-rows: auto auto;
    gap: 1rem;
    width: 100%;

    @media (min-width: 64em) {
      grid-template-columns: 65% 35%;
      grid-template-rows: auto;
    }

    p {
      grid-column: 1;
      font-size: clamp(2rem, 1vw + 1.8rem, 2.66rem);
      --text-weight: 700;
      text-wrap: balance;
      line-height: 1.3;
    }

    em {
      color: var(--theme-highlight);
      font-style: normal;
      --text-weight: 700;
    }

    img,
    svg {
      grid-column: 1;
      grid-row: 2;
      width: 70%;
      margin: 0 auto;
      object-fit: cover;
      transform: rotate(-6deg);

      @media (min-width: 64em) {
        grid-column: 2;
        grid-row: 1;
        width: 100%;
        margin: 0;
      }
    }
  }
</style>
